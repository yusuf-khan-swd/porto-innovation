import Carousel from "../Carousel/Carousel";
import Navbar from "../Navbar/Navbar";

const Home = () => {
  return (
    <div className="min-h-screen">
      <Navbar></Navbar>
      <Carousel></Carousel>
    </div>
  );
};

export default Home;